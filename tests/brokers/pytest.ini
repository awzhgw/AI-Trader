[pytest]
# 默认命令行选项
addopts = -v --tb=short --color=yes -p no:cacheprovider

# 过滤警告
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning:.*futu.*

# 注册自定义标记
markers =
    real_connection: 标记需要真实连接的测试
    skip_trade: 标记跳过买卖操作的测试
